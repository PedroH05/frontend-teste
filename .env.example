# Copiar para .env.local e preencher — nenhum valor real entra no Git.

# Supabase → Settings → API → Project URL / anon public key
# Usados SÓ pelo SDK de login (signInWithPassword, resetPasswordForEmail).
# Dado de negócio nunca passa por aqui — vai tudo pela captacao-api.
NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_ANON_KEY=""

# URL da captacao-api (local: http://localhost:3000)
NEXT_PUBLIC_API_URL=""
